import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
	padding: 1rem;
`;

const Explore = () => {
	return <Wrapper>This is the Explore component</Wrapper>;
};

export default Explore;
