import React from 'react';
import styled from "styled-components";

const Wrapper = styled.div`
	padding: 1rem;
`;

const Profile = () => {
	return (
		<Wrapper>This is the Profile component</Wrapper>
  )
}

export default Profile;
